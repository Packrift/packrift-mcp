{
  "name": "packrift-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for Packrift (Shopify) catalog, pricing, inventory, and checkout helpers.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "node dist/node.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.2",
    "hono": "^4.12.18",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "@types/node": "^25.7.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.95.0"
  }
}
